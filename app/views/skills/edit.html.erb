<h1>Editing skill</h1>

<% form_for([@skill.profile, @skill], :html => {:multipart => true }) do |f| %>
  <%= f.error_messages %>
   <%= render f%>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @skill %> |
<%= link_to 'Back', skills_path %>