-if current_user
  .textaccount
    = "Bienvenue: " + current_user.username + " !:)"
    (#{current_user.role_symbols.map(&:to_s) * ','}) 
    = link_to 'déconnexion', logout_path 
    = link_to 'modifier compte', edit_user_path(current_user)

