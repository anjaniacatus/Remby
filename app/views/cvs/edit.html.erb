<h1>Editing cv</h1>

<% form_for([@cv.civil_status, @cv]) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', civil_status_cvs_path(@cv) %> |
<%= link_to 'Back', civil_status_cvs_path %>
