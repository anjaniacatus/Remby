<h1>New qualification</h1>

<% form_for(@qualification) do |f| %>
  <%= f.error_messages %>
  <%= render f %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', qualifications_path %>