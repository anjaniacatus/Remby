<h1>Editing job</h1>

<% form_for (@job), :html => { :multipart => true } do |f| %>
  <%= f.error_messages %>

  <%= render f %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @job %> |
<%= link_to 'Back', jobs_path %>