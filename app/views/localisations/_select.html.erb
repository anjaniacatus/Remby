<%= f.label :localisation_id, "Choisir le lieu de travail" %><br />

<% params = (defined?(optional) ? { :include_blank => optional } : { :include_blank => "n/a" }) %>

<%= f.collection_select(:localisation_id, Localisation.ordered, :id, :name, params) %>
<br />
<span class="help">Si le lieu n'est pas encore référencé, remplir le champ ci-dessous.</span><br />
<span class="help">exemple: [Antananarivo]</span><br />
<%= f.label :my_localisation, "Nouveau base du poste" %><br />
<%= f.text_field :my_localisation %>