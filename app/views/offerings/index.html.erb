<h1>Listing offerings</h1>

<table>
  <tr>
    <th>Ref</th>
    <th>Object</th>
    <th>Conditions</th>
    <th>Dead line</th>
    <th>Society</th>
  </tr>

<% @offerings.each do |offering| %>
  <tr>
    <td><%=h offering.ref %></td>
    <td><%=h offering.object %></td>
    <td><%=h offering.conditions %></td>
    <td><%=h offering.dead_line %></td>
    <td><%=h offering.society %></td>
    <td><%= link_to 'Show', offering %></td>
    <td><%= link_to 'Edit', edit_offering_path(offering) %></td>
    <td><%= link_to 'Destroy', offering, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New offering', new_offering_path %>