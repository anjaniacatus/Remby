<h1>New job</h1>

<% form_for([@job.society, @job], :html => { :multipart => true }) do |f| %>
  <%= f.error_messages %>

  <%= render f %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', jobs_path %>