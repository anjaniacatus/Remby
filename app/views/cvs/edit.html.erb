<%- @title =  "edit" + @cv.title %>
<% form_for(@cv) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @cv %> |
<%= link_to 'Back', cvs_path %>
