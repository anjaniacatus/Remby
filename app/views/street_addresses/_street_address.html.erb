<% if street_address.description.blank? %>
  <%== #{street_address.class.human_name}: #{street_address.value} %>
<% else %>
   <%== #{street_address.description}: #{street_address.value} %>
<% end%>

