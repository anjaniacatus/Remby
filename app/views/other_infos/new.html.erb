<h1>New other_info</h1>

<% form_for([@other_info.profile, @other_info]) do |f| %>
  <%= f.error_messages %>
  <%= render f%>
   <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>
<%= link_to 'Back', other_infos_path %>