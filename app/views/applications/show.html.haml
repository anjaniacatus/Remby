%p
  %b Job:
  = h @application.job.ref
%p
  %b Cv:
  = link_to @application.cv.title, cv_path(@application.cv) 
%p
  %b Title:
  = h @application.title
= link_to 'Edit', edit_job_application_path(@job, @application)
|
\#{link_to 'Back', jobs_path}
