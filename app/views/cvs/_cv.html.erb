<%- unless current_user && current_user.roles == "member" %>
  <%- if cv.published == true %>
    <div class="photo" float="right">
       <%= image_tag fleximage_path(cv.civil_status)%>
       <%=h cv.title %>
       <%=h cv.summary %>
    </div>
  
    <div id = "field" >
      <%=h"Etude effectué "%>
      <%- cv.degree_courses.each do |dc| %>
      <p>
        <%= dc.field.field_name %>
      </p> 
     <% end %>
   </div>   
    <% @civil_status = cv.civil_status %>
    <%= link_to 'Show', civil_status_cv_path(@civil_status, cv) %>
 <% end %> 

<% else %>
  <% if current_user && belongs_to_user?(cv) && cv.published == true %>
    <%=h cv.title %>
    <%=h cv.summary %>
    <% @civil_status = cv.civil_status %>
    <%= link_to 'Show', civil_status_cv_path(@civil_status, cv) %>
  <% end %> 
 <% end %>
