<h1>New contact_info</h1>

<% form_for(@contact_info) do |f| %>
  <%= f.error_messages %>
  <%= render f %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', contact_infos_path %>